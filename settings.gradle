pluginManagement {
    repositories {
        // Local pre-fetched Maven repo (offline build): ~/workspace/m2repo
        maven { url = uri("file:///home/hatch/workspace/m2repo") }
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        // Local pre-fetched Maven repo (offline build): ~/workspace/m2repo
        maven { url = uri("file:///home/hatch/workspace/m2repo") }
        google()
        mavenCentral()
    }
}
rootProject.name = "DesiRadio"
include ':app'
